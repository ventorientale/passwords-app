import {EncryptDecryptInterface} from '../interfaces/encrypt-decrypt-interface';

export class AesOfb implements EncryptDecryptInterface {
  decrypt(key: string, data: string): string {
    return '';
  }

  encrypt(key: string, data: string): string {
    return '';
  }
}
